node_modules/
dist/
*.log
.DS_Store
coverage/
*.tsbuildinfo

# Local Claude Code / MCP editor config — not part of the package.
/.claude/
/.mcp.json

# Local memory stores / scratch data
*.thrift.db
*.thrift.jsonl
/.thrift-data/
/memory/
/tmp/

# Private launch, evaluation, and local design artifacts.
/.quality*/
/qa/
/drafts/
/*LAUNCH-DRAFT.md
/DESIGN*.md
/USER-GUIDE.md
/benchmark/results.md

# Binary doc/guide artifacts — generated deliverables, not OSS source.
# Keep the source repo text-only (SVG/MD stay tracked; these do not).
*.pdf
*.docx
*.pptx
