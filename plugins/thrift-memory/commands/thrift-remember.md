---
description: Store a durable memory via the Thrift MCP server (org scope by default)
---

Store the following as a memory using the Thrift `remember` tool. Default to `org` scope (fleet-wide) unless the text is clearly agent- or session-specific, in which case use the appropriate scope. Keep it short and factual — one reusable decision, convention, or fact per memory. Confirm what was stored and in which scope.

Memory: $ARGUMENTS
